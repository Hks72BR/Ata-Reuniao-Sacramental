import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hks72br.atasacramental',
  appName: 'Ata Sacramental',
  webDir: 'dist'
};

export default config;
